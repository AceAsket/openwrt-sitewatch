services:
  sitewatch:
    build: .
    image: openwrt-sitewatch:local
    container_name: sitewatch
    ports:
      - "8095:8095"
      - "8096:8096/tcp"
      - "8096:8096/udp"
    volumes:
      - sitewatch-data:/data
      - ./logs:/logs:ro
    environment:
      SITEWATCH_PROXY: "http://host.docker.internal:20171"
      SITEWATCH_DNSMASQ_CONTROL: "0"
      SITEWATCH_LOG_FILES: "/logs/pihole.log /logs/dnsmasq.log /logs/messages"
      SITEWATCH_PIHOLE_API_ENABLED: "0"
      SITEWATCH_PIHOLE_URL: ""
      SITEWATCH_PIHOLE_PASSWORD: ""
      SITEWATCH_PIHOLE_LOOKBACK: "600"
      SITEWATCH_PIHOLE_LIMIT: "1000"
      SITEWATCH_PIHOLE_DISK: "0"
      SITEWATCH_PROBE_TARGET: ""
      SITEWATCH_PROBE_PORTS: "3478,443,50000,55000,60000,65000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  sitewatch-data:
